<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite trafico HTTP sin cifrar (cleartext) solo hacia el backend de desarrollo local,
  nunca de forma general. Android bloquea cleartext por defecto desde API 28.
  10.0.2.2 es el alias que el emulador AVD resuelve al loopback del host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
